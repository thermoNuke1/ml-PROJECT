# numpy, scikit-learn, matplotlib, and torch are typically pre-installed in Colab.
# pandas and the packages below are not always present and are installed explicitly.
pandas>=1.5.0
python-chess>=1.999
xgboost>=2.0.0
orjson>=3.10.0
xxhash>=3.5.0
joblib>=1.3.0
